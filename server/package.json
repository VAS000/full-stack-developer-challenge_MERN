{
  "name": "infoxchange-full-stack-developer-challenge-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=prod node src/server.js",
    "dev": "NODE_ENV=dev nodemon src/server.js",
    "local": "NODE_ENV=local nodemon src/server.js",
    "test": "NODE_ENV=test jest --detectOpenHandles --coverage --runInBand",
    "test:watch": "NODE_ENV=test jest --detectOpenHandles --coverage --runInBand --watch",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.2",
    "jest-serial-runner": "^1.1.0",
    "jest-stare": "^2.2.0",
    "mocha": "^8.2.1",
    "mongodb-memory-server": "^6.9.2",
    "nodemon": "^2.0.3",
    "superagent": "^6.1.0",
    "supertest": "^6.0.0"
  },
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^3.22.0",
    "mongoose": "^5.9.11",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": "10.x"
  }
}
