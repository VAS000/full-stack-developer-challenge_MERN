import { createSlice } from '@reduxjs/toolkit';

const authorsSlice = createSlice({
  name: 'authors',
  initialState: {

  },
  reducers: {
    
  }
});

// export const { setAuthors } = authSlice.actions;

export default authorsSlice.reducer;